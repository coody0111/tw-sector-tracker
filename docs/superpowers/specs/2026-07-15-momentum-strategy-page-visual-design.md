# 逆轟策略頁面 — 視覺設計 Design Spec

**日期**：2026-07-15
**作者**：Developer（與 Cody 用 `ui-ux-pro-max` skill brainstorming 後）
**狀態**：草案，待 Cody review
**取代**：`docs/superpowers/specs/2026-07-14-momentum-strategy-page-design.md` §4「頁面資訊架構草案」
（該文其餘章節——B1~B6 資料層規格、落差稽核、已知資料缺口——維持有效，不受本文影響，本文只補
「本文不含實際視覺呈現」那句話留下的空白）。

---

## 背景

`2026-07-14-momentum-strategy-page-design.md` 定義的 B1~B5 資料層（`scan_momentum_health()`、
`scan_bullish_alignment_new_high()`、`scan_consecutive_limit_up()`）已經全部實作完成（commit
`faa54b8`），但該文件的狀態表沒有回頭更新，且明講「不含實際視覺呈現」。本文是那個下一階段。

**Brainstorming 過程中兩次推翻早期方案，記錄决策脈絡**：

1. **原始方案（5 個各自獨立區塊）被推翻**：最早提案照「動能派 5 個問題」切成 5 個獨立排行榜
   （①主流族群 ②族群內最強 ③能不能進 ④該不該出 ⑤最強型態），使用者要自己在腦內把 5 張表拼起來
   才能得出結論。Cody 指出這不是「一體成形」——筆記第三十條的**五級強弱分類本身就是操作決策樹**
   （超強=隨時加碼、強=續抱、整理=反彈可出、弱=直接出清、超弱=反手放空），每一級直接對應一個動作，
   不是貼在股票旁邊的裝飾性 badge。**收斂決定：頁面核心改成單一「族群→個股→動作」清單**，①主流
   族群降級為篩選範圍（不是結論），②③④收斂成同一張表裡每檔股票的欄位，⑤獨立維持（全市場掃描，
   不受族群篩選限制）。
2. **「照抄回測 backtest.py 就能驗證這些訊號」被推翻**：Cody 指出 B2（出場三原則）用現有
   `run_backtest()`（固定 D+1 進場、固定 D+1+H 出場）**框架性地測不了**——`run_backtest()` 本身
   的 plan 早就把「規則出場」列為 Out of scope。B1/B3/B4/B5 理論上可套但也有真實偏誤（還原股價
   缺口、universe 非 point-in-time、regime 相依）。**本次不處理回測驗證**，那是另一個獨立任務，
   不在本文範圍。
3. **筆記覆核發現的兩個結構性缺口，補回設計**：Cody 追問「大盤好/大盤不好怎麼辦、停利停損呢」，
   逐條覆核筆記後發現原設計完全沒處理「大盤 regime 如何改變整頁姿態」與「停利停損的哲學是什麼」，
   詳見 §3、§4。

---

## 視覺風格（沿用既有系統，不自創）

延續本專案 2026-07-15 最新一版族群總覽頁 mockup
（`docs/superpowers/mockups/2026-07-15-index-v18-inline-expand.html`）已確立的設計 token，不重新
發明一套色系/字體：

```css
--bg:#080B12; --panel:#0F1420; --panel-2:#161D2C; --panel-3:#1E2738;
--border:#293346; --border-2:#37435C;
--ink:#DADFE8; --ink-2:#98A0B4; --ink-3:#636B80;
--up:#E6432F; --down:#37B25C;                    /* 紅漲綠跌，Cody 2026-07-14 已定案的方案 */
--accent:#F0BB55; --accent-dim:#B98A3A;
--tier-super:#F0BB55; --tier-strong:#4FC46A; --tier-mid:#8B94AC;
--tier-weak:#E08A3E; --tier-superweak:#E6432F;
--serif: Georgia,"Iowan Old Style","Source Serif 4","Noto Serif TC",serif;   /* 標題 */
--sans: "Public Sans",-apple-system,"PingFang TC","Microsoft JhengHei","Segoe UI",sans-serif; /* 內文 */
--mono: ui-monospace,"IBM Plex Mono","Cascadia Code","Roboto Mono",monospace; /* 數字/代號 */
```

- 深色為主，`:root[data-theme="light"]` 提供對應淺色 token（跟現有頁面一致的明暗切換模式）。
- 五級 tier 顏色（`--tier-*`）跟族群總覽頁的族群熱度分級**共用同一組色票**，全站「超強/強/整理/弱/
  超弱」用色一致，不會因為換頁面而讓使用者重新學習一套新的顏色語意。
- 數字一律 `tabular-nums`（`.tabular` class），確保欄位對齊。

**架構決定**：本頁新開一個獨立 generator（暫定 `export/momentum_generator.py`），不強行共用
`chips_generator.py`/`html_generator.py` 的既有元件——資料形狀（`rs_rank_pct`、
`exit_3_rule_triggered`、`limit_up_streak` 等）跟既有頁面完全不同，硬套會增加既有 generator 的
複雜度。CSS 設計 token（顏色/字體變數）可以複製共用，但 HTML 結構與 JS 邏輯獨立成檔，比照
`chips_generator.py` 自成一檔的既有慣例。

---

## 3. 頁面結構（取代舊 spec §4 的 5 區塊草案）

由上而下、單頁垂直捲動，共 5 個區塊：

```
逆轟策略.html
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[大盤狀態橫幅]        大漲/小漲/持平/小跌/大跌 + 資金集中度診斷
                       決定下面 [換股警戒區塊] 是否出現、決定橫幅文案基調

[停利/停損哲學提示]    固定顯示的說明條——解釋本頁為什麼沒有停利/停損%欄位

① 今天資金在哪？       族群強度排名（橫向 pill，點選＝篩選範圍，非結論）
                       來源：既有 calc_meta_performance/calc_cumulative_meta

[換股警戒區塊]         ⚠ 僅大盤=小跌/大跌時顯示
                       弱中透強候選（可換入）vs 跌停鎖死（立刻砍）對照表

②③④ 族群→個股→動作    主表：族群內每檔股票一列，五級判斷+動作合一
  （合併為單一清單）    來源：scan_momentum_health()
                       點列展開：進場/出場三原則勾選 + 均線實際值 + 相對強弱%

⑤ 有沒有最強型態？     連續鎖漲停榜（全市場，不受①篩選限制）
                       來源：scan_consecutive_limit_up()
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 3.1 為什麼不是舊 spec 的「5 個並列區塊」

同族群裡「誰最強」「能不能進」「該不該出」三個問題，答案必須**同時**作用在同一檔股票身上才有意義
（筆記十二：「不需要分散產業，買最強的那一籃」；筆記三十：「同族群中選最強那一檔」）——拆成三張表
會強迫使用者自己拿著三份清單交叉比對。收斂成一張表後，②族群內排名、③進場確認、④出場警示三者
共同收斂成 tier 欄位的**單一動作結論**，細節（命中哪幾條）留給展開列顯示，而不是預設就攤在主表上
造成資訊過載。

---

## 4. 大盤狀態橫幅（新增，回應筆記「大盤好/大盤不好」的應對邏輯）

### 資料來源

`processors/performance.py::classify_market_regime(taiex_change_pct, breadth_ratio, divergence)`
（既有、已實作、2026-07-09 完成），回傳 `{tier, is_concentrated, concentration_direction}`，
`tier ∈ {大漲, 小漲, 持平, 小跌, 大跌}`。

### 視覺與文案分兩種模式

| 模式 | 觸發條件 | 橫幅左邊框色 | 文案基調 |
|---|---|---|---|
| **平穩模式**（calm） | `tier ∈ {大漲, 小漲, 持平}` | `var(--tier-strong)`（綠） | 照正常①-⑤流程操作，不需啟動換股警戒 |
| **急殺模式**（selloff） | `tier ∈ {小跌, 大跌}` | `var(--tier-superweak)`（紅） | 「不是清倉不動的時候」——找還在紅盤/跌得比大盤少的個股換股集中，附帶下方 [換股警戒區塊] |

### 文案依據（明列筆記出處，供 Cody/未來維護者對照原文）

- 平穩模式：無特殊文案，僅顯示大盤數字。
- 急殺模式：「大盤急殺，不是清倉不動的時候——先看還在紅盤、跌得比大盤少的個股，把弱勢股（尤其
  跌停鎖死）換到最強的1～2檔，資金集中不分散。急殺撬開最後那幾檔鎖漲停時是換股的黃金時間，不是
  逃跑時機。」
  依據：筆記一（買紅不買綠：大盤大跌時的正確動作）、筆記二十三（換股操作實戰）、筆記十四（大殺盤
  情緒點抓法——僅取用其「找最後撐住的強股」核心邏輯，**不含**盤中掛定價單等執行細節，那屬於
  `2026-07-09-momentum-notes-scan-mapping.md` 已排除的「盤中戰術」範圍）。

### 橫幅顯示內容

```
[tier 文字，紅/綠/灰]  [加權指數漲跌% · 上漲家數比 breadth_ratio]   [資金集中度診斷 badge]
[展開的說明文字段落，依模式顯示對應文案 + 出處來源]
```

`concentration_direction`（`None`/`權值股撐盤`/`中小型輪動`）直接顯示成右側 badge，供使用者判斷
這波是真的普漲還是少數權值股撐盤（呼應大盤分級儀表板 spec 原文的用途）。

---

## 5. 停利/停損哲學提示（新增，回應筆記對固定 % 停利停損的立場）

### 為什麼需要這個區塊

覆核筆記後確認：**動能派沒有「停損 X%」「停利 Y%」這種固定百分比機制**。停損就是出場三原則本身
（筆記三：「三個條件同時滿足才出場」，跟價格跌幅百分比無關）；停利也不存在固定目標價——賺的部位
「漲時加碼，跌時全出」（筆記七、八），靠倉位紀律而非價格目標控制風險。唯一例外是筆記三十二（週K/
月K 格局判斷）的「買進當天低點」防守價，但那是**另一套用於長線格局判斷的機制**，跟本頁日線動能
掃描的主邏輯不同層級，本次不做成頁面欄位，避免跟主流程混淆。

若頁面完全不提這件事，使用者會自然預期看到「停損-8%／停利+20%」這類欄位，找不到會誤以為是遺漏。
**這個提示區塊的作用是把「沒有」講清楚，變成刻意的設計决定，不是疏漏。**

### 顯示文案

> 本頁沒有停利/停損百分比欄位——動能派不用固定%，出場三原則本身就是停損機制（跌破五日線+五日線
> 下彎+重挫長黑，三者同時成立才出，不是價格跌到某%才砍）。倉位紀律取代停利：分批進場、往上加碼、
> 絕不往下攤平，賺的時候讓它繼續跑，直到出場三原則觸發為止。

固定顯示在大盤狀態橫幅之下、五級 legend 之上，不可收合（首次上線先常駐顯示；若之後使用者反應
太佔版面，可以改成一次性 tooltip 或 FAQ 連結，但**不在本次範圍內預先設計收合邏輯**，YAGNI）。

---

## 6. 換股警戒區塊（新增，僅急殺模式顯示）

### 觸發條件

大盤狀態橫幅判定為 `急殺模式`（`classify_market_regime().tier ∈ {小跌, 大跌}`）時才渲染這個區塊；
平穩模式完全不顯示（不是顯示空清單，是整個區塊不存在，避免平時版面雜訊）。

### 資料邏輯：弱中透強候選 vs 跌停立刻砍

- **弱中透強候選**：個股今日跌幅 明顯小於 大盤同期跌幅（呼應筆記三十三「弱中透強接刀法」核心
  邏輯：「大盤兩次下殺，某檔沒有創新低＝弱中透強」；本頁簡化為單日相對表現，不做筆記原文的兩次
  下殺比對——那需要盤中逐筆資料，超出本頁日線掃描範圍）。判斷欄位沿用 B4 `rs_market_score`
  （個股近5日報酬 − universe 等權平均近5日報酬，`2026-07-14` spec §3.4 已定義，這裡取其邏輯而非
  重新定義新欄位——`rs_market_score` 本來就是「跟大盤比」的相對強弱，急殺當天的個股表現只是它的
  一個特例輸入）。
- **跌停鎖死·立刻砍**：`change_pct` 觸及跌停常數（`_LIMIT_DOWN_PCT`，見 §11.3 定案）。

### 顯示形式

一張精簡對照表（股票/今日漲跌/vs大盤 pp 差距/判斷結論二選一：`弱中透強·可換入` 或
`跌停鎖死·立刻砍`），表格下方一行動作提示：「賣出『跌停鎖死·立刻砍』的持股，資金集中換進『弱中
透強·可換入』——同天執行，不分批猶豫」（依據筆記二十三换股實戰的操作節奏，換股是同天內完成，
不是分批試探）。

---

## 7. ②③④ 主表：族群→個股→動作合一清單

### 7.1 族群篩選 pill（區塊①）

橫向捲動的 pill 列表，每個 pill 顯示：族群名稱、族群排名（`#1`/`#2`...）、族群當日漲跌%。
點選 pill 切換下方主表篩選範圍，預設選中族群強度排名第一名。

⚠️ **2026-07-16 修正**：mockup 上示意過「連3日主流」這類輔助說明文字，查證後確認**現有程式碼
沒有「族群連續維持強勢天數」的計算邏輯**（`processors/performance.py` 全文搜尋無此概念），這不是
消費端能簡單推導的東西，需要額外的資料層工作。**本次先不做**，pill 只顯示排名/當日漲跌%，見
§11.4。

### 7.2 主表欄位

| 欄位 | 資料來源 | 顯示形式 |
|---|---|---|
| 股票（代號+名稱） | `scan_momentum_health()` 輸出 `stock_id`/`stock_name` | 代號用 mono 小字、名稱用 serif |
| 現價 | `close` | tabular mono |
| 漲跌% | `change_pct` | tabular mono，紅漲綠跌 |
| 族群內排名 | `rs_rank_pct`（`scan_momentum_health()` 既有回傳，0~1 百分位小數，1.0=族群內最強）驅動進度條；`#N/total` 形式由消費端（generator）對同一批輸出依 `meta_sector` groupby 後自行排序/計數取得——**已確認不需要修改 `scan_momentum_health()`**，這是純消費端邏輯，不是資料層缺口 | `#N/total` + 進度條並存 |
| 五級判斷＋動作 | `strength_tier`（`scan_momentum_health()` 既有欄位）映射到動作文字（見 §7.3） | tier chip，色點+文字 |
| 展開箭頭 | 純前端狀態 | 點列展開/收合 |

### 7.3 五級 → 動作文字映射（明列，避免實作時各自詮釋）

| `strength_tier` | 動作文字 | 額外條件 |
|---|---|---|
| 超強 | 超強·隨時加碼 | 無 |
| 強 | 強·續抱 | 無 |
| 整理 | 整理·反彈可出 | 無 |
| 弱 | 弱·直接出清 | 無 |
| 超弱 | **預設**：超弱·直接出清觀望；**若當日 `change_pct` 觸及跌停**：超弱·跌停可反手放空 | 放空文案需額外查當日是否跌停（見 §7.4） |

### 7.4 放空條件收斂（筆記三十四的限定）

筆記原文：「逆轟不空漲停的股票，只空跌停的股票」——不是任何「超弱」都建議放空。`strength_tier`
判斷本身只吃均線排列/相對強弱，不包含「今天是否跌停」這個資訊，因此**動作文字判斷需要額外讀
`change_pct` 是否觸及跌停常數 `_LIMIT_DOWN_PCT`**（見 §11.3 定案），兩個條件（`strength_tier
== '超弱'` 且 `change_pct <= _LIMIT_DOWN_PCT`）同時成立才顯示「反手放空」字樣，否則超弱只顯示
「直接出清觀望」，不主動建議放空方向。

### 7.5 個股展開面板（點列觸發）

三欄並排（窄螢幕 <900px 時改直向堆疊）：

**欄1：進場訊號 / 出場三原則**

⚠️ **2026-07-16 覆核 code 後兩輪修正**：

**第一輪**：原草案把進場欄設計成「帶量長紅突破前高／創波段新高／短中長期多頭排列」三條逐一勾選，
跟筆記十一原文字面對應，但**這跟現有資料模型對不上**——`scan_momentum_health()` 的
`entry_confirmed` 定義是「多頭排列 且 MA5/MA10 皆上揚」（舊 spec §3.2 自己就寫明這是「附加在既有
型態訊號上的**確認 badge**」，不是獨立的進場三原則），不能拆成筆記字面那三條。

**第二輪**：原本想拉進 `screener/patterns.py::detect_breakout_confirm(df)`（既有「多頭拐點」）
當第二個進場訊號補位，但追查它的出身後**決定拿掉**：查 git log 確認 `detect_breakout_confirm`
於 2026-06-28（commit `cbce208`）跟雙底/頭肩底/三角/楔型等古典技術分析型態偵測器一起寫入，
docstring 明寫「Weinstein Stage 2 起漲」——這是史坦·溫斯坦的階段分析理論，一套完全獨立於
逆轟大的技術分析流派，均線用 **MA5/MA20/MA60**（不是逆轟策略統一用的 MA5/MA10/MA60——逆轟大
筆記全文搜尋「20日/MA20/月線」**零命中**，筆記二「關鍵均線觀察」只列 5MA/10MA/60MA 三條，
20MA 從未出現在他的方法論裡）。逆轟相關工作最早的 commit（`f54d0ec`）是 2026-07-02，比
`detect_breakout_confirm` 晚了 4 天——這支函式從一開始寫的時候根本不知道逆轟這套東西存在，
是通用型態工具箱的一員，07-14 那份舊 spec 才把它借來當「並列既有型態訊號」用。這個頁面的前提
是「忠實呈現逆轟大的方法論」，混進一個不同流派、不同均線口徑的訊號會造成使用者混淆（可能看到
「拐點=否、動能確認=是」表面矛盾，實際只是兩套均線定義不同），**跟改動風險也不成比例**（
`detect_breakout_confirm` 被三處既有程式碼共用，改均線組合會動到既有型態回測，超出本頁範圍）。
**決定：進場訊號不採用 `detect_breakout_confirm`，只保留兩個真正源自逆轟筆記、且均線口徑一致
（皆為 MA5/MA10/MA60）的訊號**：

- **展開面板「進場訊號」顯示兩個燈號**：
  1. 多頭排列＋創新高（是否出現在 `scan_bullish_alignment_new_high()` 今日輸出——批次函式，
     回傳「命中的股票清單」，不是逐股 bool 欄位，消費端需把當日輸出轉成 `stock_id` 集合，用
     「是否在集合內」當作這檔股票今天的判斷）。⚠️ 這個訊號目前**不含量能確認**，跟筆記「帶量
     長紅突破」有落差，是已知資料層缺口，本次不修，見 §11.2b。
  2. 動能確認：MA5／MA10 皆上揚（`entry_confirmed` 拆解，即 `ma_alignment=='多頭排列' and
     ma5_rising and ma10_rising`——目前只有合併後的 `entry_confirmed` bool，若要逐條顯示
     MA5/MA10 個別是否上揚，需要在 `scan_momentum_health()` 額外回傳兩個子欄位，這點留給下一
     階段 plan 決定是否值得為此多開欄位，或展開面板直接顯示「動能確認：是/否」單一燈號即可，
     不強行拆到最細）
- 出場三原則維持原設計：跌破五日線／五日線下彎／重挫長黑，逐條勾選——**這三條沒有上述問題**，
  `exit_3_rule_triggered` 的三個子條件（`close<ma5`／`ma5_slope_down`／`change_pct<=閾值`）本來
  就是筆記原文逐字對應，且都是 `scan_momentum_health()` 內部已經算出來的中間值，跟 entry 那邊
  「三個不同函式拼起來」的情況不同，一樣建議額外回傳子欄位方便展開面板逐條顯示，不用改判斷邏輯。

**欄2：均線數值（今日）**
- 收盤價 + MA5/MA10/MA20/MA60 實際數字，價格高於/低於該均線用紅/綠上色（沿用漲跌色語意，
  「站上均線」視覺上等同「相對均線是漲的」）。
- 說明文字：「MA20 純參考，不參與多頭/空頭排列判斷」（呼應 spec §1.1 的收斂決定，避免使用者
  誤以為 MA20 也是判斷依據之一）。
- 資料來源：`scan_momentum_health()` 既有回傳的 `ma5`/`ma10`/`ma20`/`ma60`。

**欄3：相對強弱（近5日）**
- vs 族群平均（pp 差距）、族群內百分位、vs 大盤等權（pp 差距）三個數字。
- 資料來源：`rs_score`/`rs_rank_pct`/`rs_market_score`（`scan_momentum_health()` 既有回傳）。

**設計原則**：主表只給結論（tier chip），展開面板給證據（三原則命中細節 + 實際數字），使用者
可以自行核對系統判斷對不對，不是被迫全盤信任一個黑箱分數（呼應本專案在進貨分/回測校準工作中
一貫的「不隱藏依據」原則）。

---

## 8. ⑤ 最強型態（連續鎖漲停榜）

維持舊 spec 原設計，全市場掃描、不受①族群篩選限制（筆記三十六：連續漲停是跨族群的「最強型態」，
不因為它不在今天的主流族群裡就該被忽略）。

- 資料來源：`scan_consecutive_limit_up()`，依 `limit_up_streak` 降冪排列。
- 顯示：橫向卡片，股票名稱/代號、連板數（大字強調）、量縮鎖死狀態（`volume_declining_streak`
  為 `True` 時標「量縮鎖死」，`False` 標「出量中」，`None`——streak<2——不特別標示）。
- **新增 `breakout_volume_confirmed` 標記**（見 §8.1），卡片上額外顯示「起漲出量／量沒跟上」小字，
  跟量縮鎖死狀態並列——兩者是笔记里兩個不同階段的量能訊號，不是同一件事，見 §8.1 說明。

### 8.1 B5 新增：連板起漲日量能確認（2026-07-16 審查中發現的缺口，Cody 拍板現在就設計修法）

⚠️ **這是資料層（B5）的改動，比 §11.2b 的 B3 缺口更值得優先修**——「最強型態」榜若混進假突破，
會直接誤導使用者追一檔量沒跟上的假強勢股，風險比 B3 高。**本文只設計修法，實際改 code＋補測試
留給 `writing-plans` 階段，不在 brainstorming 階段直接動既有已上線函式**。

**筆記依據**：兩個階段的量能訊號，現有 `scan_consecutive_limit_up()` 只做對第二階段：

1. **起漲階段（現有函式缺這塊）**：筆記四十五「超強勢股切入五要件」第(1)條——「突破平台整理...
   連續出量拉3根以上漲停」「量沒出來＝假突破機率高，不追」。連板串**開始**的那幾天必須是真的
   放量，量沒出來大概率是假突破。
2. **確立強勢後（現有函式已正確實作）**：筆記三十六/三十九——「漲停鎖死+靜止不出量＝最強型態」，
   確立強勢後量能萎縮（惜售）反而是好事。這就是現有 `volume_declining_streak` 欄位。

`volume_declining_streak` 只比較連板期間**日與日之間**的量能是否遞減，沒有驗證連板**起點那天**
本身相對它自己的歷史基準是否放量——一檔起漲日量很淡（假突破）的股票，只要接下來幾天量持續更淡
（相對遞減），現有邏輯仍會判 `volume_declining_streak=True`，被當成「最強型態」推出去，但筆記的
判斷邏輯是這種假突破「不追」。

**修法設計**：

- 新增回傳欄位 `breakout_volume_confirmed`（`bool | None`）。
- 算法：取連板串**起點那天**（`limit_up_streak` 回溯到的第一天，即現有程式碼變數 `i+1` 那筆）
  的 `volume`，跟它**之前** 20 個交易日的均量比較。門檻沿用專案既有慣例
  （`screener/signals.py::scan_volume_turnover()` 的 `vol_multiple>=1.5`、
  `screener/patterns.py::detect_breakout_confirm()` 的「近10日爆量>20日均量×1.5」皆用 1.5 倍，
  這次沿用同一個數字，不另外發明新門檻）：
  - 起點當天量 ≥ 20日均量 × 1.5 → `True`（真的出量啟動）
  - 起點當天量 < 20日均量 × 1.5 → `False`（量沒跟上，疑似假突破）
  - 起點前歷史不足 20 個交易日（新股）→ `None`（無法判斷，比照 `volume_declining_streak`
    對 `streak<2` 回 `None` 的既有慣例，缺資料時老實說不知道，不猜）
- **是標記，不是過濾**：`scan_consecutive_limit_up()` 仍回傳所有符合連板條件的股票，不因為
  `breakout_volume_confirmed=False` 就從清單剔除——沿用本文一貫「不隱藏依據，讓使用者自己判斷」
  的原則（§7.5 已述），頁面上用文字/顏色區分「起漲出量」和「量沒跟上」，不是靜默篩掉。
- **向下相容**：只新增欄位，不改變 `limit_up_streak`／`volume_declining_streak` 既有語意與計算
  邏輯，既有測試不受影響。

---

## 9. 明確排除範圍（Out of scope，避免下一階段誤以為漏做）

- **盤中戰術**（當沖判斷、處置股細節、定價單掛法）：`2026-07-09-momentum-notes-scan-mapping.md`
  一開始就排除，本文延續排除，不在頁面上呈現任何盤中執行細節。
- **回測驗證**：B1~B5 五層訊號「到底賺不賺」需要另外處理（B2 出場三原則甚至連現有
  `run_backtest()` 框架都測不了，需要新的「規則出場」回測邏輯），是獨立的下一個任務，不在本文
  視覺設計範圍內，也不阻擋本文先落地。
- **買進當天低點防守價**（筆記三十二）：屬於週K/月K 格局判斷的另一套機制，本次不做成頁面欄位。
- **持股清單/實際倉位追蹤**：專案目前沒有任何「使用者手上持有哪些股票」的資料結構（已確認
  全專案無 watchlist/portfolio 相關程式碼），本頁是全市場/全族群掃描結果，不是「你的持股健檢」，
  ④出場判斷是「這檔股票現在的訊號是什麼」，不是「根據你的成本價算你賺賠多少」。
- **弱中透強的完整版本**（筆記三十三原文「兩次下殺比對」）：本文 §6 只做簡化的單日相對表現版本，
  不做逐次下殺的盤中追蹤比對。

---

## 10. Mockup 產出記錄

Brainstorming 過程中產出 3 版可互動 HTML mockup（存於對話 Artifact，未落地存檔到
`docs/superpowers/mockups/`——按專案慣例應在 Cody 確認本文後，將定案版本另存一份到該目錄並 push，
留一份可追溯的視覺記錄，這一步待本文 review 通過後一併執行）：

- v1：族群→個股→動作單一清單雛型，五級 chip + 展開三原則勾選清單。
- v2：加大盤狀態橫幅（可切換平穩/急殺兩種模式示範）、停利停損哲學提示、換股警戒區塊、放空條件
  收緊（跌停才顯示反手放空）。
- v3：展開面板加均線實際數值 + 相對強弱百分比三欄並排，取代純勾選的二值判斷。

---

## 11. 接口細節定案（2026-07-16 查證 code 後補齊，不再是開放問題）

寫作本文當下（07-15）留了 4 個「留給下一階段決定」的缺口，07-16 逐一查證現有程式碼後定案如下，
不再是開放問題，`writing-plans` 階段可以直接依此寫任務：

### 11.1 進場面板的資料來源（原缺口1，經兩輪修正，最終比原草案更精簡）

已在 §7.5 修正並詳述，這裡摘要結論：進場面板**不是**單一函式的三個子欄位，最終只保留兩個真正
源自逆轟筆記、均線口徑一致的訊號——`scan_bullish_alignment_new_high()`（是否在今日輸出清單，
B3，MA5/10/60）、`entry_confirmed`（`scan_momentum_health()` 既有欄位，當「動能確認」燈號，
同樣 MA5/10/60）。**不採用** `patterns.py::detect_breakout_confirm()`（既有「多頭拐點」）——
查證是 2026-06-28 跟古典型態偵測器一起寫入的 Weinstein Stage 2 通用工具（均線用 MA5/20/60），
早於逆轟相關工作（2026-07-02），均線口徑跟逆轟筆記（全文未曾出現 MA20）不一致，混進來會造成
「表面矛盾」的展開面板（拐點=否、動能確認=是，實際只是兩套均線定義不同），且改動它的均線組合
風險超出本頁範圍（該函式被 `screener/patterns.py` 三處既有程式碼共用，會動到既有型態回測）。
**不需要新增或修改任何既有函式的回傳欄位**，兩個訊號來源都已存在，純粹是消費端（新 generator）
要呼叫兩支函式並組合結果。

出場三原則維持原判斷：`exit_3_rule_triggered` 建議額外拆出三個子欄位（`close<ma5`／
`ma5_slope_down`／`change_pct<=閾值`）方便展開面板逐條勾選——**這個需要修改
`scan_momentum_health()` 回傳形狀**（新增欄位，不改變既有 `exit_3_rule_triggered` 欄位語意，
向後相容），是本文唯一需要動到既有函式介面的地方。

### 11.2 族群內排名（原缺口2）

**已確認不需要修改任何現有函式**。`rs_rank_pct` 用於進度條寬度；`#N/total` 由消費端
（generator）對 `scan_momentum_health()` 同一批輸出依 `meta_sector` groupby 計數/排序取得，
純消費端邏輯。已更新進 §7.2 表格。

### 11.2b B3「多頭排列＋創新高」完全沒有量能確認（2026-07-16 審查中新發現，本次不修）

⚠️ **這是資料層（B3）本身的缺口，不是本文視覺設計能修的，記錄在此留給後續任務，本次頁面照原定
資料源上線，不因此卡關**。

查證 `screener/signals.py::scan_bullish_alignment_new_high()` 實際程式碼確認：判斷條件只有
「多頭排列（純收盤價比較）」+「創新高（純收盤價比較）」兩者皆為**純價格條件**，函式全文沒有讀取
`volume` 欄位、沒有呼叫任何量能相關 helper（`patterns.py::_calc_vol_price_score`、
`screener/signals.py::scan_volume_turnover()` 的 `vol_multiple` 邏輯皆未被引用）。

但兩份逆轟筆記（`notes/動能派學習筆記.md` 筆記十一「進場三原則」第一條、`notes/每日心得日誌.md`
第104/522/536行）都一致把「**帶量**長紅突破前高/頸線」列為進場核心條件，第30行更明講核心判斷依據
是「股價、K棒、均線、**量能**」四項並列，不是可省略的加分項。**舊 spec §3.6（B6）寫「量能確認已
對上、不用新建」，列出 `_calc_vol_price_score`/`vol_multiple` 這些既有函式，但那句話只確認專案裡
某處存在量能邏輯，從未具體核對 B3 有沒有真的接上任一個——這次審查確認沒有接上，B6 那條「已對上」
的結論對 B3 來說是不成立的。**

**影響範圍**：本頁 §7.5 展開面板「進場訊號」第一項（多頭排列＋創新高／B3清單內）目前只反映純價格
突破，不反映筆記要求的量能確認，跟筆記字面有落差。

**這次的決定（Cody 2026-07-16 拍板）**：不在本文/本次修 B3，頁面依現有 B3 輸出上線；日後若要
補上量能門檻，需要另開任務修改 `scan_bullish_alignment_new_high()`（例如比照
`scan_volume_turnover()` 加一個 `vol_multiple >= 1.5` 之類的過濾條件），會改變它現有輸出集合、
需要新測試，屬於資料層改動，不在本文視覺設計範圍內。

### 11.3 跌停判斷常數（原缺口3）

**目前不存在**——`screener/signals.py:35` 只有 `_LIMIT_UP_PCT = 9.5`（漲停），沒有對稱的跌停
常數。台股漲跌停幅度本身是對稱的（同為 10% 動態限制），`writing-plans` 階段需要新增
`_LIMIT_DOWN_PCT = -9.5`（或依現有 `_LIMIT_UP_PCT` 命名慣例對稱命名），供 §6（換股警戒的「跌停
鎖死·立刻砍」判斷）與 §7.4（放空條件的跌停確認）共用同一個常數，避免兩處各自寫死數字、日後校準
時忘記改其中一處。

### 11.4 族群 pill「連N日主流」（原缺口4）

**目前不存在，且沒有可沿用的既有邏輯**——`processors/performance.py` 全文搜尋確認沒有任何
「族群連續維持強勢/主流地位天數」的計算。這不是消費端能簡單推導出來的東西（需要逐日回溯族群
排名歷史），屬於真正的資料層新增需求。**决定：本次先不做這個輔助文字**，§7.1 的族群 pill
先只顯示排名/當日漲跌%，拿掉「連3日主流」這種需要額外資料層支援的說明文字（mockup 上是示意，
不是已確認可實作的欄位）。若之後要加回，需要另開一個小任務算「族群強度排名的連續天數」，
不在本次範圍內，YAGNI。
