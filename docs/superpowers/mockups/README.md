# index.html 重新設計 — Mockup 沿革

存放 `docs/index.html`（族群總覽頁）重新設計過程中做的 HTML mockup，依時間序保留每一版，方便換平台/換 session 後接續討論，不用重做。用瀏覽器直接開啟 `.html` 檔即可看外觀（不需要跑 server）。

## 版本

1. **2026-07-07-index-v1-flat-ranklist.html** — 第一版方向定案：扁平排行清單取代三層卡片，兩層點擊，左側排行清單＋右側明細面板，桌機/手機兩種版面。深色系（沿用 chips.html/patterns.html 既有色票）。Cody 認可此方向。

2. **2026-07-08-index-v2-anomaly-dark.html** — 新增「異動族群」模組：獨立於例行排行之外，只顯示同時符合多重訊號的族群。兩種訊號類型：
   - **爆量暴衝**：`vol_ratio ≥ 1.5` 且 `|昨日排名-今日排名| ≥ 10`
   - **連續穩步噴出**：`streak ≥ 5`（連漲≥5日）且 `cum7 > cum5 > cum3`（累積報酬持續加速）

   仍是深色系。

3. **2026-07-08-index-v3-cream-ledger.html** — Cody 要求改成米色/質感路線（提到之前有做過米色版但已遺失）。改用「財經帳冊」調性：羊皮紙米色背景、serif 標題（Georgia系）、帳冊墨藍作結構強調色（不是紅/綠，不跟漲跌撞色）、赭黃/酒紫給異動族群兩種訊號語意。刻意避開「米色+赤陶色」這種常見 AI 樣板配色組合。Cody 認可此配色方向（"這就滿像的"），但要求質感再提升、且要重新探索族群排行的呈現方式（不要只是清單）。

4. **2026-07-08-index-v4-heatmap-vs-bar.html** — 目前最新版。沿用 v3 米色帳冊配色，質感細節提升（紙紋理背景、更講究的懸浮陰影層次、更細緻的分隔線）。新增可切換的兩種族群排行呈現方式（用今天真實 41 族群完整資料）：
   - **熱區格（Heatmap）**：41 族群一次攤開，色塊深淺=漲跌幅度
   - **發散長條排行**：由中線延伸的橫條，長度=漲跌幅度，由上到下排序

   **Cody 反饋「資訊不夠多」**——具體提出想看到的資訊：突然爆發的族群跟上週的差異，以及這週表現好的族群上週表現如何。

5. **2026-07-09-index-v5-week-accel.html** — 回應上一版的「資訊不夠多」反饋。新增「本週 vs 上週」欄位：
   - 用真實資料驗算範例：功率半導體上週 -4.17%、本週 +17.76%，加速度 +21.93pt（複利計算，本週=最近5個交易日、上週=再往前5個交易日，跟現有 `cum5`/`get_rolling_returns` 的 rolling window 慣例一致，不是自然週 Mon-Fri）
   - 視覺呈現：小型雙點折線圖（slopegraph）——左點=上週、右點=本週，線的顏色隨加速方向變化（紅=加速、綠=減速），旁邊標加速度數字（百分點）
   - 熱區格跟發散長條排行都有這個新欄位；異動族群卡片的「連續噴出」說明文字也從「7d>5d>3d」改成更直白的「上週X% → 本週Y%，加速+Zpt」
   - 全 41 族群真實資料：功率半導體/機器人自動化/半導體材料 加速度最高（+21.9/+12.8/+11.3pt），被動元件/太陽能/載板 正在減速（負值）
   - 加速度單位確認為 `pt`（percentage point，兩個百分比直接相減），不是相對百分比變化，避免跟一般 `%` 混淆
   - 週期定義確認：Cody 要的是「上週三到本週三」這種同星期幾對同星期幾的概念，跟現有滾動5個交易日算法本來就是同一件事，不用改；不需要嚴格對齊日曆週一~週五

6. **2026-07-09-index-v6-clarity-audit.html** — Cody 提出核心原則：「所有數據都是我懂得、我可以用到的」，不要有看不懂的裸數字。逐項校對 v5 修正：
   - 爆量暴衝卡片拿掉重複的裸數字「排名跳動+19」（跟上面「昨日#21→今日#2」重複，且無單位）
   - 「量比1.1x」→「今日量能1.1x於5日均量」，補上比較基準
   - 漲跌檔數「▲9 ▼6」→「▲9檔 ▼6檔」，補單位（熱區格+長條排行都改）
   - 發散長條排行新增欄位標題列（名次/族群/今日漲跌%/漲跌家數/上週→本週），原本完全沒有欄名
   - 順便修正晶圓代工卡片原本只顯示「今日排名#3」缺昨日對照的不一致，補成「昨日#15→今日#3」

7. **2026-07-14-index-v7-signal-fidelity.html** — Debugger review v6 後提出 5 個具體問題，逐項修正（設計評論見同日 `debug-tasks.md`/對話紀錄，未另開 spec）：
   - **Slopegraph 刻度失真**：原本用全部族群的 min/max 當刻度，功率半導體 +21.9pt 的極端值把其他族群的線拉成幾乎看不出角度的假平線。改成固定 ±8pt clamp，超過封頂但方向保留，一般族群的小幅加速/減速終於看得出真的線形。
   - **顏色語意衝突**：slopegraph 原本用紅/綠表加速/減速，跟熱區格本身「今日漲跌」的紅/綠語意衝突（同一個 tile 可能今日跌但本週比上週加速）。線條改中性墨色，方向改由旁邊的加速度數字顏色單獨表達。
   - **熱區格沒有任何判讀說明、看不出排名**：加一條圖例說明條（色塊=今日漲跌、角標=排名、底部折線=加速度）+ 每格右上角加排名角標。
   - **長條排行族群名可能截斷**：欄寬 112px→132px，加 `title` 屬性當 fallback；表頭改 sticky，捲動到後段仍看得到欄名。
   - **異動族群橫向捲動沒有視覺提示**：加右側漸層遮罩 + 「可左右滑動」提示文字。

8. **2026-07-14-index-v8-magnitude-encodings.html** — Cody 反饋 v7 的「發散長條」在真實資料範圍很窄
   （這批 41 族群今日漲跌% 只有 -2.93%~+2.03%）時，長條長度差異小到看不出來，長度這個視覺通道沒
   被用滿。並排 3 種方案比較（同一批真實資料，只拆「族群排行」單獨比較，不含完整頁面其他區塊）：
   - **① 窄版長條**：延續發散長條語彙，上限從 40%縮到 22%，欄位變窄
   - **② 熱力膠囊**：拿掉長度通道，改用固定寬度色塊＋顏色濃淡（跟熱區格同一套色階邏輯）＋數字疊字
   - **③ 固定刻度圓點**（bullet chart）：軌道固定 ±3% domain，用點的位置表達數值，不受任何離群值
     影響（呼應 v7 修過的 slopegraph 刻度失真問題，這次直接用固定 domain 從根本避開同類問題）
   - **尚未定案**：三選一還是其他組合，待 Cody 看過三版實際渲染後反饋

9. **2026-07-14-index-v9-heatgrid-only.html** — 族群排行呈現方式**定案：熱區格**。Cody 看完 v8
   的三種列表方案後反饋「方格比較好」，直接拍板用熱區格，發散長條／v8 的窄版長條／熱力膠囊／固定
   刻度圓點都不採用。這版拿掉切換鈕，只留熱區格；順帶把色階從 10%~44% 加強到 16%~78%（這批資料
   範圍窄，舊色階深淺差異不夠明顯，既然定案只用這個呈現方式，可讀性更重要，直接加強）。異動族群、
   排名角標、slopegraph 固定刻度修正都原封不動沿用 v7。

10. **2026-07-14-index-v10-week-context.html** — Cody 反饋每格需要直接看到「上週跟這週的情況」，
    原本只有 slopegraph 形狀 + 加速度數字，要自己推算原始數字。新增「上週% → 本週%」兩個原始數字
    （tabular-nums，各自紅/綠上色），今日漲跌家數（▲/▼檔數）確認維持不變、位置沒動。其餘沿用 v9。

11. **2026-07-14-index-v11-dark-drilldown.html** — 兩個方向合併測試，Cody 要求「有點質感」：
    - **色系脫離米色帳冊**：改深色暖黑（`#17130E`，帶褐不是藍黑）＋銅金強調色，不是 `DESIGN.md`
      那種通用深藍灰 dashboard；亮色主題也對應改成暖紙色（不是純米色重複）。
    - **版面改三欄**：左＝精簡排行清單（由上到下，只留名次/族群/今日%，不像熱區格塞那麼多資訊）、
      中＝異動族群（改直向堆疊取代橫向捲動，補一段「本週最強加速」次要資訊）、右＝**個股明細面板**
      （點左側族群即時切換）——**這是 v1~v10 都沒畫過的「兩層點擊」缺口，這版第一次真的做出來**。
    - **已知限制（誠實記錄，不是完成品）**：個股明細目前只有前 3 名族群（功率半導體/網通設備/
      晶圓代工）準備了真實個股名單當範例，其餘族群點擊會顯示「明細待補」——這是刻意的，不是每個
      族群都硬塞假資料；正式版要接 `calc_stock_sparklines` 之類的真實函式，不是這次 mockup 的範圍。
    - 熱區格（v9/v10 定案）跟三欄+個股明細（v11）目前是兩條並行的探索方向，**還沒決定最終要選哪個
      或如何合併**，待 Cody 反饋。
    - **後續多輪調色**（同一份檔案上直接迭代，未另開版號）：底色暖褐黑→深藍黑、文字色脫離米色系
      改中性冷灰白、漲跌色最終改回熱區格米色版原始色票（`--up:#A8321F` 磚紅／`--down:#2F6B3F`
      森林綠，Cody 貼 `data/photo_for_test/色塊.png` 指名）、排行清單改整列底色（不是小色塊）且
      飽和度對齊熱區格。目前 `data-theme` 固定深色預設，不跟系統自動切換（見檔案內註解）。

12. **2026-07-14-index-v12-merged-heat-drilldown.html** — Cody 問「合併長怎樣」，直接做出熱區格
    ＋v11 個股明細的合併版：**維持熱區格全寬、一次看完 41 個族群**（不像 v11 三欄把排行擠成窄清單），
    **點擊任一族群格子，下方展開該族群的個股明細面板**（沿用 v11 表格設計）並自動捲動過去；再點
    同一格或按「收合」關閉面板。異動族群回到橫向捲動（全寬空間夠用，不用像 v11 中欄那樣改直向堆疊）。
    色系沿用 v11 最新定案（深藍黑＋銅金＋磚紅/森林綠）。個股明細一樣只有前 3 名族群有範例資料。

13. **2026-07-14-index-v13-richer-cards.html** — Cody 要求卡片資訊更多，核心訴求是「看得出族群
    是持續走強還是弱掉」，不是單純塞數字。重新做資訊分層：
    - **新增「動能狀態」五級標籤**（超強/強/整理/弱/超弱）：在族群層級用「連漲天數＋本週比上週是否
      加速」綜合判斷，呼應（但未直接共用程式碼）逆轟策略 `scan_momentum_health` 已定義的五級分類
      邏輯——這是這版最核心的新增，直接回答「持續中還是弱掉了」而不是只看單日漲跌。
    - **新增連漲/連跌天數**：直接標「連漲6日」/「連跌9日」，比只看今日%更能看出持續性。
    - **新增法人連買 badge**（外資/投信連買≥2日才顯示）、**新增量能異常 badge**（量比≥1.5x才顯示）。
    - 上週→本週原始數字保留但降到卡片最下面當輔助佐證，不再是主要資訊。
    - 卡片因此變大（168px→224px 起跳），一頁能看到的族群數變少，這是資訊量增加的必然取捨。
    - **誠實限制**：法人連買天數／量比目前是示範用的合理範例數值，沒有接真實 `institutional`/
      `daily_prices` 查詢；動能狀態標籤的判斷規則是這次先訂的草案，之後接線時應該讓族群層級分類
      重用 `scan_momentum_health` 的同一套規則常數，不要各寫一份（避免兩邊定義drift）。
    - **後續校對+調色**（同一份檔案上直接迭代）：族群名防截斷+加強對比、連漲/連跌天數與週數字
      改照漲跌色系上色（原本死板灰字）、漲跌色/強調色整體調亮調飽和（Cody 反饋「不夠鮮豔」）、
      「▲/▼幾檔」也補上色。

14. **2026-07-14-index-v14-temp-change.html** — Cody 問「之前不是還有 pt 的嗎」：v9~v12 都有的
    「本週比上週加速度」(pt) 在 v13 重做卡片時被拿掉了，這版做回來：
    - **新增溫度變化徽章**（🔥增溫／❄️退燒，|accel|≥5pt 才顯示）：刻意用橙/藍的「溫度」視覺語言，
      跟今日漲跌的紅/綠色系分開——因為這是兩件不同的事，一個族群今天可能還是紅的，但已經在退燒
      （呼應 Cody 自己的用詞）。避免重蹈 v7 修過的同類錯誤（slopegraph 曾經跟漲跌色系撞在一起）。
    - **新增「族群近況」一句話**（Cody 要求）：把動能狀態(tier)+溫度變化(temp)綜合成人話短句
      （例如「強勢再加速」「走弱，尚未止穩」），用 serif 斜體呈現、放在卡片名稱正下方最顯眼位置，
      不用使用者自己拼湊徽章腦補。
    - **誠實限制**：溫度變化的 ±5pt 門檻、近況一句話的合成規則，都是這次先訂的草案，沒有回測或
      實跑真實資料驗證過門檻鬆緊（太鬆會每格都掛徽章、太緊會漏掉真正有意義的變化）。
    - **後續修正**：近況一句話原本用小號斜體灰字太不顯眼，Cody 反饋以為沒做，改成有底色+左側
      金色豎線的引言框樣式加強對比。

15. **2026-07-14-index-v15-sector-status-section.html** — Cody 反饋「不要塞進每格」，v14 每格
    內嵌的「族群近況」一句話拿掉，改成<b>熱區格下方獨立的「族群近況」區塊</b>，三塊組成：
    - **①升溫/退燒雙欄排行**：按 pt（本週-上週）排序 Top5，不用掃 41 格直接看到誰在加速誰在轉弱。
    - **②轉折點列表**：抓「上週等級」跟「這週等級」真的翻轉一級的族群（例如強→整理、弱→超強），
      比單純幅度排序故事性更強。**誠實限制**：真實版需要存歷史快照（每週把 `classifyTier` 結果
      寫進 DB）才算得出「上週等級」，這次 mockup 用 `PREV_TIER_OVERRIDE` 手動指定 6 檔製造示範
      故事，沒被指定的族群視為「等級沒變」，不是真實計算結果。
    - **③角色說明**：新增提示框區分「異動族群」（頁面最上方，抓瞬間訊號：爆量+排名跳動，今天
      vs 昨天）跟「族群近況」（抓週度趨勢：加速度+等級翻轉，這週 vs 上週）——兩者容易被誤會是
      重複資訊，故意分開兩區塊並說明時間尺度不同。
    - **🔴 校正（同版補回）**：Cody 發現頁面最上方原本的「⚡ 異動族群」快報消失了。查證後確認
      這是 v13 整版重做熱區格資訊密度時，重心全放在卡片本身、沒把 v7~v12 就有的異動族群橫向
      捲動區塊（`.anomaly-strip`）搬過去，v14、v15 的說明文字都誤植成「它還在頁面最上方」但
      實際 markup 一路缺席到現在才發現。已在這版把區塊（含 CSS class 跟 5 張範例卡）補回族群
      排行上方的原始位置，並移除卡片上失效的 `onclick="selectGroup(...)"`（v13 起就沒有對應
      的 drilldown 函式，點了會噴 JS 錯誤，只留 hover 效果）。

16. **2026-07-15-index-v16-stock-drilldown.html** — Cody 提醒「記得點開要有個股列表」。查證後
    發現這也是 v13 整版重做卡片時跟異動族群區塊一起消失的功能（v11/v12 就做過，v13 之後完全
    沒有 `selectGroup`/`detail-panel` 相關程式碼）。這版把個股清單點開面板接回來：
    - 族群排行熱區格、異動族群卡片都可點，點開會在熱區格下方展開該族群個股清單（股號/股名/
      收盤/漲跌%+幅度長條），再點一次同一張卡收合。
    - **誠實限制（沿用 v11/v12 就有的限制，還沒解決）**：只有異動族群那 5 檔族群（網通設備/
      晶圓代工/功率半導體/機器人自動化/半導體材料）補了範例個股資料，其餘 36 個族群點開會顯示
      「還沒補資料」的提示；真正要接線需要 `calc_stock_sparklines` 或等價的個股層級查詢，這份
      `STOCKS` 常數只是展示排版用，資料是編造的，不能直接搬進生產程式碼。

17/18. **2026-07-15-index-v17-drawer-panel.html** / **2026-07-15-index-v18-inline-expand.html**
    — Cody 反饋 v16 的點開面板「拉到下面去很不直覺」：面板固定接在整個熱區格最後面，點越前面
    排的卡片，視線要滑過的距離越長，跟點擊位置完全脫節。做兩版方向比較，供 Cody 選：
    - **v17（右側滑出 drawer）**：點卡片後面板從螢幕右側固定位置滑出（`position:fixed` +
      `transform` 動畫），背後加半透明遮罩，點遮罩或收合按鈕關閉。**優點**：不管點哪張卡視線
      移動距離都一樣，完全不用捲動頁面，連續點不同族群體驗一致。**代價**：會遮住/擠壓右側一
      部分熱區格內容；窄螢幕（&lt;640px）面板佔滿全寬，接近全螢幕覆蓋。實作簡單（固定定位，
      不用算版面）。
    - **v18（卡片原地展開）**：面板動態插入到「被點卡片所在那一整排」正下方（JS 用
      `offsetTop` 分組算出同一排的卡片，`grid-column:1/-1` 撐滿整排寬度插進去）。**優點**：
      空間錨定感最好，點哪裡就在哪裡展開，不遮住任何內容；點異動族群卡片也會自動捲到熱區格
      對應那一排展開，直接展示兩區塊指向同一份資料。**代價**：實作邏輯較複雜（欄數隨螢幕寬度
      變化的 row 分組計算）；連續點不同族群時，版面高度會因為展開位置不同而一直跳動。
    - 兩版都改掉 v16 用 `.hidden` class 切換顯示/隱藏的做法：v17 改用 `transform` 滑動狀態，
      v18 改用動態插入/移除 DOM 節點（因為面板要出現的位置每次都不一樣，不能是固定佔位的
      隱藏節點）。
    - **🏆 定案：v18（卡片原地展開）**。之後接進 `export/html_generator.py` 時以 v18 的
      `offsetTop` 分組插入邏輯為準，v17 保留當備選方案紀錄。

19. **2026-07-16-index-v19-visual-polish.html** — v18 定案後 Cody 要求「改一下 UI 設計」，
    選定三個方向打磨（點開展開方式維持 v18 邏輯不變，只動視覺）：
    - **視覺深度**：topbar 加漸層+陰影跟頁面主體拉出層次；三大區塊（異動族群/族群排行/族群近況）
      加極淡背景漸層分區（`.zone`），掃過頁面時能感覺到「換了一個問題」但不會被切成孤立方塊；
      異動族群卡片左側色條改漸層、hover 加陰影。
    - **資訊層次重整**：熱區格卡片的 tier（動能等級）跟 temp（溫度變化）原本是兩個同等份量的
      pill 互相搶注意力，改成 tier 維持 pill（主角）、temp 降成無底色純文字（輔助提示）；
      連漲天數/法人徽章/量能三種資訊合併成一行、用細直線分隔，徽章文字精簡（「外資連買5日」→
      「外資5日」），密度不變但視覺更俐落。
    - **個股面板打磨**：面板加獨立漸層 header 區塊、表格列間距/hover 更明確，個股漲跌長條改用
      `currentColor` 漸層取代純色塊，多加「平均漲跌」摘要數字給整體印象。

20. **2026-07-16-index-v20-font-legibility-fix.html** — Cody 反饋「族群名稱有點不太明顯」+
    問「字體有辦法改變嗎」。查證後是**字型層級的根本問題**：`--serif` 原本 Georgia 起頭、中文
    後備字是「Noto Serif TC」（Google Font，一般電腦不會裝），中文字元最後落到瀏覽器通用
    serif，Windows 上通常是很細辨識度差的細明體——全頁所有用 `--serif` 顯示的中文都受影響，
    不只熱區格一處。中文介面很難靠系統字型湊出 serif/sans 雙軌都好看，改成兩者統一用「微軟
    正黑體」（Windows 內建、字重完整）當中文首選，層次感改用字重/字級/字距做，不再依賴字型
    家族差異。**順便修一個對應的顏色 bug**：卡片文字對比色只有漲跌% 數字會依「漲跌方向」
    切白色/`--ink`，族群名稱完全沒有這個邏輯；但底色飽和度是依「漲跌幅度」連續變化
    （`heatBg()` 的 alpha），不是方向二分——幅度越大的格子（越顯眼、越該看清楚）背景越飽和，
    `--ink` 對比反而越不夠。改成 `heatBg()` 回傳 `{bg, textColor}`，名稱跟漲跌% 共用同一套
    依「實際底色深淺」判斷的對比邏輯。`.ht-name` 字重也從 700 加到 800。

21. **2026-07-16-index-v21-real-serif-webfont.html** — Cody 反饋「不」「這更醜」：v20 把
    `--serif` 整批改成微軟正黑體，診斷沒錯（Noto Serif TC 沒被真的載入，只是寫在字型清單裡
    碰運氣）但修法選錯方向——正黑體是 Windows 系統 UI 預設字，拿來當標題整頁像一般 Windows
    應用程式，v6~v19 一路維持的 editorial serif 質感全部不見了，這才是「更醜」的真正原因。
    這版**真的把 Noto Serif TC 載入進來**（頁首加 Google Fonts `<link>`，鎖定 600/700/800
    三個字重對應頁面實際用到的字重，避免瀏覽器合成假粗體），`--serif` 順序改回
    `Noto Serif TC → Microsoft JhengHei → Georgia → ...`，正黑體降回次要備援。v20 的顏色
    對比修正（`heatBg()` 回傳 `{bg, textColor}`）完整保留，那部分沒被反饋說有問題。
    **已知取捨**：這是這批 mockup 系列第一次引入外部網路請求（Google Fonts CDN），之前
    v6~v20 全部自包含；之後這個字型方向若要接進 `export/html_generator.py` 正式站台，需要
    另外決定自己 host 字型檔或接受 CDN 依賴，這次先不擋著討論。

22. **2026-07-16-index-v22-revert-original-font.html** — Cody 反饋「太醜」「別用這個」，
    明確要求「用最一開始的字體吧」——確認是指 v19 之前的原始字型鎖（Georgia 起頭+通用
    serif 後備），不是 v20 的正黑體、也不是 v21 的 Google Fonts Noto Serif TC。
    **🏆 定案：字型完整退回 v19 原始值**，拿掉 Google Fonts `<link>`，回到純自包含檔案
    （跟 v6~v19 一致）。v20/v21 診斷的「Noto Serif TC 沒被真的載入、中文字元會落到瀏覽器
    通用 serif 後備」這個技術事實仍然成立，但 Cody 的判斷是這樣呈現本身沒問題、不需要為了
    字型正確性去換掉看習慣的樣子。v20 引入的**顏色對比修正**（`heatBg()` 回傳
    `{bg, textColor}`）跟 `.ht-name` 字重 700→800 都沒被反饋說有問題，繼續保留——
    真正解決「族群名稱不明顯」的是這個顏色修正，不是字型本身。

23. **2026-07-23-index-v23-stock-decision-panel.html** — Cody 問「個股資訊還缺少什麼以及可以改的地方」後，先做一版桌機 mockup，不動正式 `docs/index.html`：
    - **個股面板從資料表改成決策面板**：保留 v18/v22 的「卡片原地展開」方向，但展開後先放「候選前 3」與候選排序規則，再放完整表格。目標是先回答「哪幾檔值得看」，不是一開始就丟一排漲跌數字。
    - **新增個股決策欄位**：候選分、判斷標籤（領頭續強/爆量突破/強勢延伸/弱勢反彈）、族群相對強弱、位置/風險、量能/流動性、法人同步。現版表格已有收盤、漲跌%、量比、5/7/10/14日，這版補的是「為什麼這檔是候選」。
    - **新增快速篩選 chips**：全部、強於族群、爆量、法人同步、突破/創高、排除過熱。mockup 內已可互動篩選，預設不再只按今日漲幅排序，而是以示範用 candidate score 排序。
    - **資料誠實限制**：數字以 2026-07-23 生成頁面中「工業電腦」現有個股列為基底，再補示範用相對強弱、成交額、位置風險、法人細節。這些新增欄位還沒有接 `export/index_generator.py` 的真實計算，正式化時需要定義 candidate score 與每個狀態標籤的規則。
    - **2026-07-23 同版修正**：Cody 反饋「精簡一點，規則定義寫在 spec，不要出現在網站上」以及「族群字色改壞」。移除頁面可見的 mockup 說明、Decision lens、候選排序權重與規則提示，只留下候選結果/篩選/表格；族群熱區格明確鎖定 button 與族群名稱淺色，避免瀏覽器預設 button text color 讓族群名變黑。
    - **2026-07-23 歷史延續探索**：Cody 想看「這個族群很棒上排行榜後，下週是否能延續」的小型歷史區分。先試過在熱區格/面板上方放連續上榜、新上榜、回榜等 badge，但 Cody 指出可以放最下面，類似「上上週哪一天出現過」。已改成面板底部的「歷史出現紀錄」：列出 07/09（四）上上週 #18、07/16（四）上週 #9、07/23（四）本週 #1，旁邊保留近5週排行軌跡。規則判定仍留給 spec，不在頁面上解釋。
    - **2026-07-23 卡片文字層級修正**：Cody 覺得族群卡片「大標題小標題都是白色」層級不夠。調整熱區格文字色：排名降淡、族群名維持暖白主視覺、漲跌%改用淡紅/淡綠方向色、小標籤與週資料降成灰階輔助，避免整張卡所有文字同等亮度。

24. **2026-07-23-index-v24-clean-followup-panel.html** — Cody 直接反饋「UI 設計還是有點醜」。這版不再沿著 v23 疊補，而是重新降噪：
    - **族群卡從紅綠大底色改成中性卡面**：漲跌方向只用卡片上緣線與數字色表示，避免整排紅綠色塊像警示牌。族群名、漲跌%、排名、輔助標籤有明確明度層級。
    - **展開面板改成左候選、右表格**：今日首選獨立成左側主卡，另外兩檔用緊湊列補充；右側保留篩選與表格，少掉 v23 的三張大候選卡互相搶注意力。
    - **保留重要的新概念**：底部加入「歷史出現紀錄」與「昨日候選追蹤」。當族群隔天轉弱時，昨天標記的個股不消失，而是進到追蹤表，用抗跌、突破失敗、過熱回落等狀態驗證訊號是否延續。
    - **設計定位**：v24 是比 v23 更接近正式產品方向的視覺稿；v23 保留作為概念探索紀錄。

25. **2026-07-23-index-v25-workbench-followup-panel.html** — Cody 反饋 v24 仍然「有點醜」後，再往正式工具頁收斂：
    - **少一點展示感，多一點工作台感**：字體改回乾淨 sans，拿掉偏 editorial 的標題質感；整頁由黑底亮線改成深灰分層，降低報表/簡報味。
    - **族群卡改成方向側線**：不再用整張卡或上緣紅綠去搶視線，漲跌方向以左側細線與漲跌% 顏色表示，選中族群用低調金色邊線。
    - **規則說明繼續留在 spec**：頁面底部的歷史區塊只保留「週次 / 排名」與「隔日驗證」短標，不在網站上解釋判定規則。
    - **延續 v24 的產品邏輯**：昨天強勢股若隔天遇到族群轉弱，會進入「昨日候選追蹤」，用抗跌、突破失敗、過熱回落等狀態呈現，而不是從今日候選直接消失。

26. **2026-07-23-index-v26-refined-official-palette.html** — Cody 反饋 v25「有點醜」，選定 v23 的
    正式色票方向後要求「重新改一下」「樣式風格」。這版由另一個 session（本次會話）審查 v23~v25
    後重做，同一功能骨架，重點修正審查抓到的問題：
    - **色票 100% 對齊已上線版本**：直接照抄 `export/index_generator.py` 目前 `:root`/
      `:root[data-theme="light"]` 的完整 token（不是憑印象重寫），包含 `--tier-*` 五級動能色、
      `--heat-cold`/`--trend` 語意色，深/亮主題都補齊，不再有 v24/v25 那種自創新色票的分裂。
    - **修正 `DESIGN.md` 明文違規**：熱區格從寫死 `repeat(5,...)` 改回 `auto-fill,minmax(224px,1fr)`；
      候選表格從「38列全部攤開的大表格」（違反「超過15行table一律改卡片/分欄」）改成
      **預設只顯示前10檔＋「顯示其餘28檔」展開按鈕**，跟這次會話已經在 `momentum.html`
      決策主表用過的「大量資料預設收合」慣例一致。
    - **熱區格拿掉整片紅/綠底色**：改成中性 `panel-3` 卡面 + 頂部細線用五級動能色（不是漲跌色）+
      漲跌%數字單獨上色——避免 v23 那種「整排紅綠色塊像警示牌」的觀感（這正是 v24 想解決但
      自己配色又出局的問題，這版用官方色票做到同樣的效果）。
    - **草案規則誠實揭露**：頁尾補一行 `.footnote`，比照既有頁面「⚠️ 門檻未經回測校準」慣例，
      明講候選分/判斷標籤/相對強弱/歷史排行都是示範用途，沒有接真實計算——不是新增文案量，
      是把審查抓到的「規則不存在」老實標注出來，而不是假裝這些數字是真的。
    - **尚未解決**：候選分/決策標籤/歷史快照這幾個核心資料缺口本身（見下方「尚未定案」），
      這版只處理視覺呈現層，沒有動到底層規則設計——那塊仍待 Cody 決定是否要做、由誰做。

27. **2026-07-23-index-v27-full-41-sectors.html** — Cody 問「除了工業電腦族群 其他族群呢？族群排序呢？」，
    v26 只做了 5 檔示範熱區格。這版從當天真實產出的 `docs/index.html` 抓出全部 41 個族群的
    排名/排名變化/漲跌%/動能分級，套進 v26 的視覺樣式：
    - **資料來源**：用腳本解析當天實際生成的 `docs/index.html` 熱區格區塊，逐一取出 41 個族群的
      `rank`、`rank_delta`（含方向）、`pct`、`tier`，確保排序（依當日 `avg_change_pct` 由高到低）
      跟真實產出一致，不是手動排列示範數字。
    - **熱區格欄位統一收斂成 4 個真實欄位**：排名+排名變化箭頭、族群名、漲跌%、動能分級 pill。
      v23~v26 示範用的投信/外資連買天數 badge、量能倍數 badge、近5日→前5日週比較行，這版**全部拿掉**——
      不是遺漏，是抓取腳本對真實 HTML 的 badge/week 區塊解析失敗（regex 對不上實際 `ht-badges`/
      `ht-week` 標記），與其讓部分族群有 fabricated 內容、部分沒有造成不一致觀感，選擇 41 檔統一
      只呈現能確認為真的欄位。這也符合現實：badge 本來就是條件式顯示（連買≥2日、量比≥1.5x等門檻），
      多數族群平常也不會有 badge。
    - **新增排名下降的顏色區分**：原本 `.rank .delta` 不分方向都套 `--up` 色，這版加
      `.rank .delta-down{color:var(--down)}`，排名退步的族群（如太陽能 ↓34、AI晶片 ↓38）用下跌色箭頭，
      不會跟排名進步的族群同色混淆。
    - **展開細節面板仍只做工業電腦一檔**：其餘 40 族群目前是靜態卡面（跟正式版一樣可點擊，但這份
      mockup 沒有接每一族群各自的候選/歷史資料），維持 v26 已標注的「候選分/決策標籤/歷史快照
      仍是示範用途」揭露原則，沒有為了填滿 41 檔而編造內容。

## 尚未定案的部分

- ~~點開個股清單的展開方式：v17（右側滑出）vs v18（卡片原地展開）還沒選定~~ → **2026-07-15
  定案 v18（卡片原地展開）**。之後這個方向要正式接進 `export/html_generator.py` 時，用
  v18 的 `offsetTop` 分組插入邏輯，不是 v16/v17 的固定位置。
- ~~族群排行最終要用哪一種呈現方式~~ → v9 曾定案熱區格，但 v11 又長出「三欄+個股明細」新方向，
  **目前兩條並行，還沒最終定案**——熱區格資訊密度高但沒有個股明細的容身之處；三欄版解決了個股
  明細，但族群排行本身變得精簡（犧牲了熱區格的「一眼看全貌」）
- 異動族群模組的規則門檻（`vol_ratio≥1.5`、排名跳動`≥10`、`streak≥5`）目前是經驗法則，沒有文獻依據，Cody 確認先這樣用，之後待回測
- ~~v6 熱區格因空間較小，暫時沒加欄位標題列~~ → v7 已加判讀說明條 + 排名角標解決
- ~~🔴 最大缺口：點進族群看個股清單完全沒設計~~ → **v11 第一次做出來，v13 整版重做時弄丟，v16 補回**，但只有 5 個族群有範例資料，其餘族群、真實資料接線都還沒做
- 目前資料都是寫死在 JS array，沒有接 `calc_meta_performance`/`calc_cumulative_meta`/`calc_stock_sparklines` 等真實計算
- `DESIGN.md`（現行深色卡片系統規範）跟這批 mockup（不管米色版或 v11 深色版）的方向都互相矛盾，若真的採用其中一條路線，`DESIGN.md` 需要正式更新，避免兩份文件同時存在造成之後誤判

## 技術路線提醒

不用 React，最終要直接改寫 `export/html_generator.py` 的 HTML/CSS/JS 生成邏輯，維持純靜態站架構。這些 mockup 目前都是純展示用途，跟正式程式碼、`data/screener.db` 沒有連接。
