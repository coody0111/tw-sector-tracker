# 量化因子入門筆記

> 給 `tw-sector-tracker` 掃盤系統的因子建立概念整理。
> 起源：從一個「進化出來」的因子 **Vol Regime Adaptive Momentum** 學到的思路，
> 整理成自己能反覆使用的方法論。目標是「學會建立因子的概念，同時改善族群掃盤」。

---

## 0. 一句話總結

> **因子 = 每天、對每個族群，算出一個數字，這個數字要能「預測」該族群未來一段時間的相對強弱。**
>
> 量化的核心不是「算出漂亮指標」，而是逼自己回答一句話：
> **「你昨天給的分數高，今天真的比較會漲嗎？還是跟丟骰子差不多？」**

---

## 1. 什麼是「因子」（放進掃盤脈絡）

掃盤系統現在已經在算一堆族群指標：族群漲幅、三大法人買超、融資變化、型態訊號、
observation scores……**這些其實全都是「候選因子」**。

差別只在於：目前是用規則/肉眼在看，還沒問過它們最重要的那句話——「這個分數到底能不能預測未來？」

學會問這句話、並用數字回答，就從「憑感覺掃盤」升級成「用驗證過的因子掃盤」。

---

## 2. 核心問題：它能「預測」嗎？

關鍵詞是**預測**，不是「解釋過去」。

- ❌ 錯的問法：這個族群過去一週漲很多（描述現在，事後諸葛）。
- ✅ 對的問法：今天因子分數高的族群，**未來 N 天**是否傾向續強？

要能回答，必須把兩件事對齊：
1. **因子值**：今天（point-in-time）能算出來的數字。
2. **未來報酬**：該族群「之後」N 天的報酬（訓練/驗證時才知道，實戰時是未來）。

---

## 3. RankIC（Rank Information Coefficient）

衡量因子預測力的主要工具。

- 做法：每一天，把所有族群「依因子值排名」，也把它們「依未來 N 天報酬排名」，
  算兩組排名的相關係數；再對所有交易日取平均。
- 解讀：
  - RankIC 為正且穩定 → 因子分數高的族群確實傾向未來較強 → **有效**。
  - RankIC ≈ 0 → 跟隨機沒兩樣 → **雜訊，該丟**。
  - **樣本外**（沒參與調參的資料）還是正的 → 才算真的有預測力，不是歷史裡好看而已。
- 用排名（rank）而非原始值，是為了對離群值穩健、也貼近「相對強弱選股」的實戰。

---

## 4. 建立因子的標準流程（5 步）

1. **假設（economic intuition）**
   先講一個有道理的故事。例：「法人連續買超的族群，接下來 5 天會續強。」
   沒有故事的因子容易是過擬合的巧合。

2. **定義計算**
   把故事變成「每天、每族群一個數字」。
   ⚠️ 嚴守 **point-in-time**：只能用當下已公布、拿得到的資料，**絕不能偷看未來**
   （前視偏誤 look-ahead bias 是回測灌水最常見的死因）。

3. **評估預測力**
   算 RankIC（見上）。也可看 IC 衰減（預測力撐幾天）、分位數多空價差
   （最高分那組 vs 最低分那組的報酬差）。

4. **組合**
   多個有效因子怎麼疊：去掉彼此高度相關（重複）的、決定加權方式，
   合成一個綜合分數。目標是「互補」而非「疊一堆相似的」。

5. **樣本外驗證**
   切一段完全沒參與調參的資料再驗一次。撐得住 → 接回掃盤當族群排序分數。

---

## 5. 範例拆解：Vol Regime Adaptive Momentum（波動率機制自適應動量）

一個「進化出來、事後回看卻有經濟直覺」的因子，拿來當範本。

**主邏輯：動量，但回看天期隨波動切換**
- 動量 = 過去漲得多的、未來傾向續強（強者恆強）。
- **高波動機制 → 用 5 日動量**：市況劇烈時資訊消化快、翻臉也快，短天期訊號新鮮、反應快。
- **低波動機制 → 用 20 日動量**：市況平靜時趨勢慢慢走，長天期平滑掉雜訊、抓穩定漂移。
- 「adaptive」的精髓：**同一個動量策略，在不同市況用不同記憶長度**（傳統動量是固定天期）。

**兩層過濾（疊在動量上，篩掉爛訊號）**
- **日內價格區間位置**：收盤價落在當天最高–最低區間的哪裡。收在接近最高 = 買方強勢、有 conviction。
- **成交量排名過濾**：只挑量能排名夠前的，確認有人參與、避免低流動性假突破。

**合起來**：機制對的動量方向 + 當天收盤夠強 + 有量撐，三者齊備才給高分。

**為什麼「進化出來」值得注意**：沒有人先寫下「高波動要用短動量」這個假設，
是演化式搜尋把基礎積木（動量／波動判斷／區間位置／量能排名）隨機組合、
留下樣本外表現好的、再變異，**自己撞出來**的——而且撞出的東西事後有經濟直覺，
不是純過擬合的亂數。

---

## 6. 誠實提醒（漂亮 Sharpe 上實盤前必壓測）

一個因子「RankIC 為正」不等於「能賺錢」，中間隔著這些關卡：

1. **交易成本 / 滑價**：換手快的因子（如 5 日動量）成本吃很兇，扣完還剩多少？
2. **多重測試偏誤**：搜了成千上萬個因子，最好那個的 Sharpe 天生被運氣灌水；
   「80% 因子樣本外為正」也可能被搜尋過程本身撐高。
3. **前視偏誤**：判斷「現在是高/低波動」「法人買超」時，用到的資訊當下是否真的拿得到。
4. **容量 / 回撤體感**：回測的 −15% 回撤，實盤連續回撤時抱不抱得住是另一回事。

> 口訣：**先把「怎麼誠實驗證一個因子」做扎實，再談要不要自動生因子。**
> 很多人栽在跳過這步、直接崇拜漂亮的 Sharpe。

---

## 7. 第一個練習：族群因子健檢台（learning by doing）

**不要**一開始就實作花俏的因子。先做這個地基工程、CP 值最高：

> 把**現有**的每個族群訊號（漲幅、法人、融資、型態…）各自當成一個因子，
> 算它們的 RankIC，排出「哪些訊號真的能預測族群強弱、哪些是雜訊」。

**為什麼先做這個**
- 逼你把 **point-in-time、RankIC、樣本外分段** 這三個地基建起來
  （之後不管做什麼因子都要用）。
- 產出立刻有用：第一次「知道」掃盤裡哪幾個訊號是真金、哪幾個該丟。
- 之後 Vol Regime Adaptive Momentum 只是「再加一個新因子丟進去比 RankIC」而已，水到渠成。

**待定的設計問題**（開始實作前要先敲定）
- 「未來報酬」看幾天？（次日 / 5 日 / 10 日 → 決定預測目標）
- 族群報酬怎麼定義（等權 vs 市值權？）
- 樣本外怎麼切（時間切分，避免用未來資料調參）。

---

## 8. 名詞速查

| 名詞 | 白話 |
|---|---|
| 因子 (factor) | 每天每族群一個、想預測未來強弱的數字 |
| RankIC | 因子排名 vs 未來報酬排名的相關性；衡量預測力 |
| point-in-time | 只用當下拿得到的資料，不偷看未來 |
| 前視偏誤 (look-ahead bias) | 不小心用了未來資訊 → 回測灌水 |
| 樣本外 (out-of-sample) | 沒參與調參的資料；真正檢驗預測力的地方 |
| 動量 (momentum) | 過去強的未來傾向續強 |
| Sharpe | 報酬 / 風險 的比值；越高越好（股票因子 >1.5 算強） |
| 多重測試偏誤 | 搜太多因子，最好那個的績效被運氣灌水 |

---

## 9. 實戰發現（2026-07，用真實 daily_prices 跑出來的）

> 用約 1.5 年（2025-01 ~ 2026-07）的台股資料、42 個族群，親手跑 RankIC → 回測 → 扣成本 → 樣本外。
> ⚠️ 全部只有 1.5 年、又逢台股 AI 大多頭，是「初步、指示性」結論，非鐵證。

### 因子健檢結果（族群層級，預測未來 10 日）

| 因子 | 全期 RankIC | 樣本外 RankIC | 判讀 |
|---|---|---|---|
| **動量_20日** | +0.163 | **+0.196** | ✅ 最強，過樣本外考試 |
| RegimeMomentum（花俏版） | +0.129 | +0.097 | ✅ 撐住但**輸給陽春動量** |
| 短期反轉_5日 | −0.076 | −0.072 | ❌ 露餡（台股族群沒反轉效應） |
| 低波動_20日 | −0.124 | −0.206 | ❌ 露餡（低波動異象在此不成立） |
| 量比_5over20 | +0.013 | −0.010 | ❌ 單看是雜訊 |

### 五個帶得走的教訓
1. **族群動量（最近一個月誰最強）是目前最可靠訊號**，有效期在 10~20 天（IC 衰減顯示越長越準），不是給當沖的。
2. **量比單看是雜訊，但當「過濾器」有效**：動量 + 量比各半平均 → 變差（+0.196→+0.112）；動量 **×** 高量過濾 → 變好（→ +0.212）。**「怎麼組合」比「組不組合」重要**：量能是用來**確認**動量，不是平均混進去。
3. **別上「聰明因子」的當**：花俏的 RegimeMomentum 在台股資料上**沒贏過**最簡單的 20 日動量。複雜度必須在樣本外打敗簡單版才值得用。
4. **別人的異象不一定成立**：短期反轉、低波動在美股很有名，台股族群這段期間都是負的。**一定要用自己的資料驗。**
5. **回測漂亮先懷疑，分清 beta vs 超額**：動量前段族群回測年化 +60~95%，但同期「無腦買全部」的基準也 +25~52%（大多頭）。扣台股成本（手續費+證交稅）後、樣本外仍**海放基準約 +40%**——**可持續的是這個「超額」，不是 headline 絕對報酬**（換市況絕對數字會縮）。

### RankIC 心法複習（用實例）
- 某天把 42 族群依動量排名 vs 未來 5 日報酬排名，兩排名相關 = 當天 RankIC；對全期取平均。
- +1=完美、0=雜訊（丟硬幣）、−1=反著用。**不是每次都準，是平均大方向對。**
- 命中率「近 7 成」= 約 7 成交易日「動量高的整體 > 動量低的整體」，講的是**相對強弱、不是單支輸贏**。

### 實務優化方向（未實作，留給下次）
- 換手率偏高（每 10 天約 45%）→ 對成本敏感。可用「降換手」（拉長換股頻率 / 加緩衝區，明顯掉出前段才換）省成本。
- 個股層 vs 族群層對照、樣本外分段、扣成本，這些之後都收進正式的「因子健檢台」模組（見 spec）。

---

_本筆記為概念整理，非實作規格。實作族群因子健檢台時另開 spec/plan。_
